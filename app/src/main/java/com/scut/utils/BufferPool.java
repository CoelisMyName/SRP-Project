package com.scut.utils;

public class BufferPool {
    /*

    int size;
    static final int BUFFER_COUNT = 50;

    int head = 0;
    int tail = 0;
    byte[][] buffer;

    Semaphore full;
    Semaphore empty;
    ReentrantLock lock;

    public BufferPool(int bufferSize) {
        size = bufferSize;
        buffer = new byte[BUFFER_COUNT + 1][size];
        empty = new Semaphore(BUFFER_COUNT);
        full = new Semaphore(0);
        lock = new ReentrantLock();
    }

    public byte[] getBuffer() {
        lock.tryLock()
    }

    public void putBuffer(byte[] bytes) {

    }

     */
}
